<% provide(:title, "Edit Student") %>
<div class="container">
  <h2 class="display-2 aheading">Edit Student</h2>
  <div class="row">
    <div class="col-md-6 col-md-offset-3">
      <%= form_for(@student, html: {class: "needs-validation", novalidate: true}) do |f| %>
        <%= render 'shared/error_messages', obj: @student %>
        <div class="form-group">
          <%= f.label :name %>
          <%= f.text_field :name, class: 'form-control' %>
        </div>
        <div class="form-group">
          <%= f.label :netID, "NetID" %>
          <%= f.text_field :netID, class: 'form-control', readonly: true %>
        </div>
        <div class="form-group">
          <%= f.label :department %>
          <% if is_admin? %>
            <%= f.select :department_id, @departments_list, {include_blank: true}, {class: "custom-select", required: true} %>
            <div class="invalid-feedback">Please select student department.</div>
          <% else %>
            <%= f.select :department_id, @departments_list, {include_blank: true}, {class: "custom-select", disabled: true} %>
            <%= f.hidden_field :department_id, value: current_user.department.id %>
          <% end %>
        </div>
        <div class="form-group">
          <%= f.label :fyp_year %>
          <%= f.select :fyp_year, @fyp_year_list, {}, {class: "custom-select"} %>
          <div class="invalid-feedback">Please select student department.</div>
        </div>
        <br>
        <%= f.submit "Save changes", class: "btn btn-primary" %>
        <%= link_to @student, method: :delete, data: { confirm: "Delete this student?" } do %>
          <button class="btn btn-danger" aria-label="Delete Student"><i class="fas fa-trash"></i>&nbsp; Delete</button>
        <% end %>
        <%= link_to "Back", '/students', {class: "btn btn-secondary"} %>
      <% end %>
    </div>
  </div>
</div>
